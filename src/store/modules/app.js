const state = {
    sidebar:{
        opened: true,
        withoutAnimation: false
    },
    device: 'desktop',
}
const mutations = {}
const actions = {}

export default {
    namespaced: true,
    state,
    mutations,
    actions
}

